type: external

allowed_gateways: [chess]

act_as:
  method: hmac
  required: true
  hmac_secret: var.actas_secret

actas_allowed_subjects: [alex, sam]

keys:
  - name: local

rules:
  files:
    chess-coach.cedar:
      path: chess-coach.cedar
